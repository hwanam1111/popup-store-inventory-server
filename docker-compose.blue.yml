version: '3'

services:
  createjump-inventory-backend-server:
    build: .
    container_name: createjump-inventory-backend-server-blue
    restart: always
    environment:
      - TZ=Asia/Seoul
    ports:
      - '3002:3000'