version: '3'

services:
  createjump-inventory-backend-server:
    build: .
    container_name: createjump-inventory-backend-server-green
    restart: always
    environment:
      - TZ=Asia/Seoul
    ports:
      - '3001:3000'